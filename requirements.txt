# Core Flask dependencies
flask==2.3.3
gunicorn==21.2.0

# Database
pymongo==4.6.0
python-dotenv==1.0.0

# Basic ML dependencies
numpy==1.24.3
scikit-learn==1.3.0

# ML dependencies - CPU only PyTorch (use extra index to keep PyPI access)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu

# Sentence transformers with minimal dependencies
sentence-transformers==2.2.2
transformers==4.35.2
tokenizers==0.15.0
huggingface-hub==0.19.4 