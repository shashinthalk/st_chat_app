# Ultra-minimal requirements for production
flask==2.3.3
gunicorn==21.2.0
pymongo==4.6.0
python-dotenv==1.0.0

# Minimal ML stack
numpy==1.24.3
scikit-learn==1.3.0

# CPU-only PyTorch (much smaller) - use extra index to keep PyPI access
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu

# Only essential transformers components
sentence-transformers==2.2.2
transformers==4.35.2
tokenizers==0.15.0
huggingface-hub==0.19.4 